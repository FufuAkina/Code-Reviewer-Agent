version: '3.8'

services:
  code-agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: code-agent
    environment:
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
    volumes:
      - ./:/app
    # 移除 restart: unless-stopped（让容器运行完就停止）
